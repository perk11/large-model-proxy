test interrupting while another service is starting
max start time to avoid monitoring processes for now
address TODOs
process monitoring

Support more endpoints
    https://api.openai.com/v1/models/{model}
    /v1/audio/speech
    /v1/audio/transcriptions
    /v1/audio/translations
    /v1/embeddings

release


fix output when args is empty
periodically check if commands stopped on their own
JSON5
logging level
bug? workdir not respected sometimes
support spaces in cmd args
check for duplicate service names in config
env variables
Allow non-local addresses
